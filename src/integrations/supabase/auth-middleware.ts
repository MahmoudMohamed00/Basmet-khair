// This project currently runs on a Vite + React Router client app, not TanStack Start.
// Keep a compatible placeholder export here so older imports do not break the build.
export const requireSupabaseAuth = () => {
  throw new Error('requireSupabaseAuth is not available in this project runtime.');
};
